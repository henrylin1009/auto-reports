# -*- coding: utf-8 -*-
"""全站唯一設定源:銀行、桶、分類、容差、模型。
新增一家銀行 / 改一個桶名 / 換模型 → 只改這裡一處(不再散落 extract_v2 / batch_v2 / bridge_v2 / make_web)。
注意:BANKS 的『名稱』與 BANK_COLORS 的鍵是 data.json 的資料鍵,改名等於改資料鍵,務必同步既有 JSON。"""

# ── 銀行:代碼 → 顯示名(唯一真相)──
BANKS = {"5835": "國泰", "5836": "富邦", "5841": "中信", "5843": "兆豐", "5847": "玉山"}

# 合併報表(AI1)白名單:只收中信,避免誤收其他家撐大批次
AI1_CODES = {"5841"}

# 企業品牌色(全站統一:個體圖表 / 估值視角 / matplotlib 皆用同一組)
BANK_COLORS = {"中信": "#046A38", "中信(合併)": "#046A38", "兆豐": "#C9A227",
               "國泰": "#00584A", "富邦": "#0072BC", "玉山": "#007A7A"}

# ── 會計分類 ──
CLASSES = ["Trading", "OCI", "AC"]
CLS_TITLE = {
    "Trading": "透過損益按公允價值衡量之金融資產明細表",
    "OCI": "透過其他綜合損益按公允價值衡量之金融資產明細表",
    "AC": "按攤銷後成本衡量之債務工具投資明細表",
}
CLS_NAME = {"Trading": "透過損益按公允價值衡量之金融資產",
            "OCI": "透過其他綜合損益按公允價值衡量之金融資產",
            "AC": "按攤銷後成本衡量之債務工具投資"}

# ── 桶(標籤表,不會長大)+ 會計意義規則(給 LLM,非關鍵詞查表)──
BUCKETS = ["公債", "貨幣市場", "公司債", "金融債", "資產基礎", "可轉讓定存單", "其他", "股票",
           "衍生", "評價調整"]

#: 「衍生」與「評價調整」**曾經是同一個桶(調整項),那是錯的**,2026-07-26 拆開。
#: 兩者在會計上是相反的東西,混在一起會算出定義上不可能的數字:
#:   衍生   —— 真實持有的資產,有市值,只是不是債券 → 算 wide,但可從「債券MV」排除
#:   評價調整 —— 不是資產,是「逐項成本 → 公允」的橋;它的存在本身就宣告其餘列是成本
#: 中信實測:把兩者一起當可排除 → 可排除 = −5,782,981 → 債券MV 215,117,416
#: **大於**類別合計 209,334,435。子集大於全集,不可能。
DERIVATIVE = "衍生"
VALUATION_ADJ = "評價調整"

#: 出現這個桶的列 ⇒ 同一份 record 的其餘列是【成本】口徑(見 oracle-basis-mismatch)。
#: 這是判別口徑的機械訊號,不看銀行/年報半年報/檔名。

BUCKET_RULES = """把每列歸到下列其一(用會計意義判,不是字面):
- 公債:中央/地方政府發行的公債(政府公債)
- 貨幣市場:短期(≤1年)票券——國庫券、商業本票、承兌匯票、短期票券
- 可轉讓定存單:可轉讓定期存單、可轉讓定存單、央行定期存單、銀行定存單、銀行定期存單
- 公司債:一般企業發行的債(含可轉換公司債)
- 金融債:銀行/金融機構發行的債(金融債券、次順位金融債)
- 資產基礎:證券化商品、資產基礎/擔保證券(ABS/MBS)、不動產抵押貸款證券、REITs
- 股票:權益性質——上市櫃/興櫃/未上市股票、受益憑證、基金、特別股、存託憑證
- 其他:表上真的印著「其他」的列。只有這種才進「其他」桶
- 衍生:衍生性金融商品——利率交換/遠期外匯/選擇權/換匯換利/期貨/信用違約交換/資產交換
- 評價調整:把逐項金額由成本橋到公允的那一列——評價調整、金融資產評價調整、
  備抵損失、累計減損、未攤銷溢折價、應計利息。**不是持有的資產,是差額。**

判不準的名目 → bucket 填 "__UNKNOWN__",不要猜、不要丟進「其他」。
「其他」是表上實際存在的科目,不是「不知道」的收容所,兩者混在一起會讓錯誤看起來像正常值。

取值欄位:桶一律取【總額 / 公允價值】欄,不是「取得成本」也不是「帳面金額」。
理由:桶取總額、扣減另計,與桶取帳面金額、扣減為 0,兩種都會通過
sum(桶)+衍生+調整==總額,自動檢查分不出來,所以必須明訂一種。"""

# ── 舊管線(extract_v2)專用,凍結,不要跟著上面演進 ────────────────────────
# extract_v2.py 用 `bucket != "調整項"` 濾非桶列。上面把「調整項」拆成
# 衍生 / 評價調整 之後,若讓它繼續共用,LLM 會吐出它認不得的桶名 → 當成真桶
# 加進去,線上網站的數字會壞掉。v3 還沒接上網站(bridge_v2 仍吃
# extract_v2_results.json),所以舊管線必須維持**逐字不變**的行為。
# R5 刪掉 extract_v2.py 時,這兩個常數跟著一起刪。
LEGACY_BUCKETS = [b for b in BUCKETS if b not in ("衍生", "評價調整")] + ["調整項"]
LEGACY_BUCKET_RULES = BUCKET_RULES.replace(
    """- 衍生:衍生性金融商品——利率交換/遠期外匯/選擇權/換匯換利/期貨/信用違約交換/資產交換
- 評價調整:把逐項金額由成本橋到公允的那一列——評價調整、金融資產評價調整、
  備抵損失、累計減損、未攤銷溢折價、應計利息。**不是持有的資產,是差額。**""",
    "- 調整項:衍生(利率交換/遠期外匯/選擇權/換匯換利/期貨)、評價調整、"
    "備抵損失、累計減損、未攤銷溢折價、應計利息").replace(
    """理由:桶取總額、扣減另計,與桶取帳面金額、扣減為 0,兩種都會通過""",
    """備抵損失、評價調整等扣減項另外進「調整項」(負數)。
理由:桶取總額、扣減進調整項,與桶取帳面金額、調整項為 0,兩種都會通過""")
assert "調整項" in LEGACY_BUCKET_RULES and "衍生:" not in LEGACY_BUCKET_RULES, \
    "BUCKET_RULES 改動使 LEGACY 替換失效 —— 舊管線會拿到 v3 桶名,先修這裡"

# ── 欄名 → 口徑 ────────────────────────────────────────────────────────────
# 這是**判斷層**的詞彙,跟同義詞表同一個生命週期(plan_v3_2_flow.md §2.3),
# 不是版型知識:它描述的是會計欄位的意義,不是某一家的排版。
# 附註通常只有一欄、欄名是日期(「113年12月31日」),口徑要靠算術推(見 slice.basis_of);
# 明細表才會把成本與公允並列 —— 所以兩表互對時,要挑「同口徑」的那一欄比。
# 「取得成本／攤銷成本」是同一個東西的另一種印法(富邦、玉山)—— 債券按攤銷後成本
# 列示,股票沒得攤銷,所以欄名把兩種寫在一起。不是第三種口徑。
COST_COLS = ("取得成本", "取得成本／攤銷成本", "取得成本／攤銷後成本")
BOOK_COLS = ("公允價值總額", "帳面金額")   # 公允 == 帳面,是同義詞不是兩種口徑

# 桶 → data.json 網站的 7 個 wide 桶(僅名稱/併桶差異)。
# **衍生 / 評價調整 刻意不在這裡**:網站 7 桶沒有對應欄位,硬塞進「其他」會讓
# 「其他」同時裝著真的印著『其他』的科目與兩種完全不同的調整項,再也拆不開。
# 恆等式因此是三段:sum(wide 7 桶) + 衍生 + 評價調整 == 類別合計。
# (衍生要不要獨立成第 8 個 wide 桶,是真的分類決定,見 plan_v3_2_flow.md §3.1,尚未拍板。)
BUCKET_MAP = {"公債": "GB", "公司債": "公司債", "金融債": "金融債",
              "資產基礎": "資產基礎", "貨幣市場": "貨幣市場",
              "可轉讓定存單": "貨幣市場",   # NCD 屬貨幣市場工具,併入(網站無獨立欄)
              "其他": "其他", "股票": "股票"}
WIDE_BUCKETS = ["GB", "公司債", "金融債", "資產基礎", "貨幣市場", "其他", "股票"]

# ── 對帳容差與節流 ──
# v3-P1:每個讀值都自證(整欄相加==印出合計),精確相等才收。容差保留當旋鈕,預設 0
# (見 docs/plan_refactor_v3.md P1.4)。
TOL = 0             # 絕對下限(仟元)
TOL_REL = 0         # 相對容差
MIN_GAP = 2.0       # 每次 API 呼叫最小間隔(秒),避開免費層 RPM
MAX_EXPAND = 3      # 單頁起最多擴 3 次頁

# ── 面板(跨期)離群:同銀行同分類,偏離鄰期中位超過此比例 → 標待複核 / 可弱錨救援 ──
PANEL_JUMP_REL = 0.40

# ── 模型 ──
MODEL = "gemini-3.5-flash-lite"   # 免費層 500/天
